import React from "react";


function Some (props) {
    return (
        <div>
            {props.email}
            <br/>
            {props.contact_no}
        </div>
    )
};

export default Some;